[Unit]
Description=Exporter script for the litecoin crawler

[Service]
WorkingDirectory=/home/ubuntu/bitnodes
ExecStart=~/.virtualenvs/bitnodes/bin/python -u export.py export.conf.default

# output goes to /var/log/syslog
StandardOutput=syslog
StandardError=syslog
User=ubuntu
Group=ubuntu

# This will default to "bundler" if we don't specify it
SyslogIdentifier=export

[Install]
WantedBy=multi-user.target
Alias=export.service

